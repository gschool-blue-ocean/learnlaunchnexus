import React from "react";
import { createRoot } from "react-dom/client";
import App from "./components/App/App";
// hoks into the DOM for the application
createRoot(document.getElementById("root")).render(<App />);
